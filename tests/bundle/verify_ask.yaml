bundle: test
variables:
  input: "tests/bundle"
  output: "tests/bundle"
actions:
  - action: 'verify'
    type: 'ask'
    source: '{input}'
    includes:
      - 'verify_data.ttl'
    queries:
      source: '{input}'
      includes:
        - '*_ask_query.rq'
    expected: false
