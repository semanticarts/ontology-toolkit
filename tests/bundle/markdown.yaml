bundle: test
variables:
  input: "tests/bundle"
  output: "tests/bundle"
actions:
- action: "markdown"
  message: "Formatting release notes."
  source: "{input}/Table.md"
  target: "{output}/Table.html"
